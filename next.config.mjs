/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['localhost','jgoteam.com','thepyoadmin.jgoteam.com'],
  },
};

export default nextConfig;
